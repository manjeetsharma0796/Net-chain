{
  "name": "@netchain/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server exposing the NetChain corporate netting ledger to any MCP-capable AI agent.",
  "type": "module",
  "bin": {
    "netchain-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  }
}
